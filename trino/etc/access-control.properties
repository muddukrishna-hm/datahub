# File-based access control — mirrors the Ranger RBAC model defined in docs/authorization.md.
# The trino-ranger plugin requires a custom Trino image; file-based control is used here for the
# dev environment. Ranger still runs for policy management UI and audit demonstration.
access-control.name=file
security.config-file=/etc/trino/rules.json
